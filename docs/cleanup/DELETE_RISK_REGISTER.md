# Delete Risk Register

**Phase 2 companion.** The actionable subset of `FILE_CLASSIFICATION_MATRIX.md` — only items with a recommendation other than KEEP_*. Nothing here is deleted yet; this is the input to Phase 10 (quarantine plan) and Phase 11 (execution). Each row states the evidence, the risk if the recommendation is wrong, and what "safe" looks like.

| Item | Recommendation | Deletion risk if wrong | Evidence for the recommendation | Safe next step |
|---|---|---|---|---|
| `ros2_ws/src/bonbon_perception/` | REMOVE_DEAD | **Very low.** Disabled launch file + emptied `console_scripts` + zero repo-wide imports means nothing can regress even if the removal is somehow wrong — it isn't reachable today. | `launch/perception.launch.py.disabled`, `setup.py` has empty `console_scripts`, `grep -r "import bonbon_perception"` repo-wide = 0 hits | Quarantine first (Phase 10) with a restore command, permanent-delete after one more pytest/grep re-check in Phase 11 |
| `Bonbon-robot/` (top-level) | REMOVE_OBSOLETE | **None.** Empty directory, 0 commits, not a git submodule of the outer repo. | `git log` inside it: no commits; no `.gitmodules` in outer repo | Safe to delete directly in Phase 11 (no quarantine value — there is no content to preserve) |
| `deploy/pi2_deployment_bundle.tar.gz` | REMOVE_GENERATED_CACHE | **None** — already gitignored, so removing it doesn't touch git history at all, only local disk. | `.gitignore:40` excludes `deploy/*.tar.gz`; no code/CI reads it | Delete directly, it isn't tracked |
| `deploy/{pi2_manifest.txt, pi2_exclude.txt, pi2_qwen_benchmark_results.json}` | REMOVE_GENERATED_CACHE (or relocate) | **Low-medium** — these ARE git-tracked, so deleting removes them from working tree but they remain in git history; still, they're one-off historical records someone might want findable in `docs/`. | Referenced only from historical docs (`docs/PI2_QWEN25_05B_SETUP_REPORT.md`, `docs/PI2_CODE_TRANSFER_REPORT.md`), not by any live code/CI | **Recommend relocating to `docs/archive/` rather than deleting** — decide in Phase 10, don't auto-delete tracked files with historical value |
| `ros2_ws/build/`, `ros2_ws/install/`, `ros2_ws/log/` | REMOVE_GENERATED_CACHE | **None** — gitignored, colcon-regenerable, and this environment doesn't even have colcon installed to regenerate from (stale from a different machine anyway). | `git check-ignore -v` confirms all 3; `find -type f` shows 2,178/158/85 files, 0 git-tracked | Delete directly in Phase 11 |
| `.mypy_cache/`, `.pytest_cache/`, `.ruff_cache/` (root + all per-package copies) | REMOVE_GENERATED_CACHE | **None** — standard regenerable tool caches, gitignored | `git status --ignored=matching` confirms every instance is ignored | Delete directly in Phase 11 |
| `ros2_ws/src/bonbon_operator_api/frontend/{node_modules,dist}/` | REMOVE_GENERATED_CACHE (local disk only) | **None** — gitignored, `npm install`/`npm run build` regenerate both; production Docker build never touches this local copy | `frontend/.gitignore` lines 1-2; `Dockerfile.dashboard-web` runs its own multi-stage build | Delete directly — saves ~571M local disk, zero git/deploy impact |
| `ros2_ws/src/bonbon_speech_ai/` | QUARANTINE_UNVERIFIED (real code, not dead — do NOT delete) | **High if deleted** — this is real, tested, working code (asr_router/tts_router/language_detector/transcript_normalizer/hospital_entity_corrector), just not wired into the live `bonbon_speech` node yet. Deleting it would destroy working code that a future task (already flagged in `docs/SPEECH_AI_UPGRADE_REPORT.md`) may want to wire in. | Confirmed real, tested, zero external importers — an orphan, not dead code | **Do not remove.** This is a "wire it in or consciously decide not to" decision, not a cleanup decision — leave for the user/a dedicated feature task |
| `ros2_ws/src/bonbon_patient_kiosk/auth/` (and `bonbon_operator_api/auth/`) | MERGE_DUPLICATE candidate (not urgent) | **Medium if merged carelessly** — genuinely different role sets (4-role viewer/operator/engineer/admin vs. 2-role staff/admin) and separate SQLite DBs; a careless merge could weaken one system's access control by accident. | `bonbon_patient_kiosk/auth/auth_manager.py`'s own docstring: "Pattern-copied from bonbon_operator_api.auth.auth_manager" | **REFACTOR_NEEDED, not a Phase 11 action** — flag for a dedicated future task to extract a shared auth library with full test coverage of both role sets; out of scope for this cleanup pass |
| `founder_command_center/` | **OUT OF SCOPE — user decision (2026-08-14)** | N/A | Confirmed unrelated purpose (CRM/business-ops), zero coupling to the robot codebase | User explicitly chose "leave it alone entirely" when asked. Excluded from every remaining phase of this cleanup — not touched, not quarantined, not counted in any before/after size comparison. |

## Explicitly NOT on this list (confirmed safe, no action needed)

- All 10 safety-critical packages (`bonbon_safety` through `bonbon_actions`) — every one is KEEP_PRODUCTION with strong evidence, see `FILE_CLASSIFICATION_MATRIX.md`.
- `bonbon_distributed_network_monitor` — initially flagged as orphaned, corrected after direct verification: genuinely runs in production on all 3 Pis via `ros2 run` inside the real `distributed-liveness`/`ros2-support` docker-compose services. Confirmed live, not on this list.
- `bonbon_hardware_telemetry`, `bonbon_edge_ai_runtime` — real, tested code, correctly wired into `launch/edge_ai/*.launch.py`, but that launch mechanism is **not part of the real production deployment** (see `DEPLOYMENT_MODE_CONFLICT_REPORT.md`). Not dead code — don't delete — but not currently running on real hardware either. This is a "wire it into the real docker-compose deployment or consciously leave it for later" decision, same category as `bonbon_speech_ai`, not a cleanup action.
- Root-level `docker-compose.{dev,robot,simulation}.yml` — confirmed legitimate thin wrappers, not duplicates.
- All 12 Git LFS-tracked binary assets — no waste, no duplicate formats found.
- `bonbon_msgs`, `bonbon_srvs` — pure interface packages, both actively depended upon.
