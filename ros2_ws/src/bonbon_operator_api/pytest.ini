[pytest]
testpaths = tests
pythonpath = .
asyncio_mode = auto
addopts = -v --tb=short
markers =
    slow: marks tests as slow
    ros2: marks tests requiring a live ROS2 environment
    integration: marks integration tests
    websocket: marks WebSocket tests
