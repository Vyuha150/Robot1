from setuptools import find_packages, setup

package_name = "bonbon_speech_ai"

setup(
    name=package_name,
    version="1.0.0",
    packages=find_packages(exclude=["tests", "tests.*"]),
    data_files=[
        ("share/ament_index/resource_index/packages", [f"resource/{package_name}"]),
        (f"share/{package_name}", ["package.xml"]),
    ],
    install_requires=["setuptools"],
    zip_safe=True,
    maintainer="BonBon Robotics",
    maintainer_email="venka@bonbon-robotics.local",
    description="ASR/TTS routing layer, language detection, transcript normalization, hospital entity correction",
    license="Proprietary",
    tests_require=["pytest"],
)
