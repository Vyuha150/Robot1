[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
; bonbon_ai_runtime on path for the Pi-efficiency scenario that exercises
; the RuntimeSelector Hailo->CPU fallback. Redundant under colcon (both
; installed together) but needed for standalone pytest.
pythonpath = . ../bonbon_ai_runtime
