[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
; AuthorityManager consumes PiId/PiLinkState from bonbon_distributed_safety
; directly rather than re-declaring them -- one definition of "which Pi,
; what link state" for the whole fleet.
pythonpath = . ../bonbon_distributed_safety
