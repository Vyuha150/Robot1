# Release Process

Releases are generated by `.github/workflows/release.yml`.

Release artifacts include:

- source/runtime archive
- SHA256 checksum
- cosign signature
- release metadata from `release_version.py`

Version format:

```text
<channel>-<UTC timestamp>-<git sha>
```

Production robots should only deploy signed artifacts from a successful release workflow.
