# Subsystem profile for local development (mock HAL, full AI stack).
profile: local_dev
simulation: true
services: [data_stores, safety, hal, vision, speech, ai, behavior, actuation, tts, operator_api]
navigation: false   # nav2 not required for desktop dev
